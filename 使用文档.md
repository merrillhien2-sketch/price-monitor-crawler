# 电商商品价格监控爬虫 - 使用文档

## 一、环境准备

### 1.1 系统要求

- Python 3.10 或更高版本（已在 3.10.12 和 3.11+ 上测试通过）
- 操作系统：Linux / macOS / Windows
- 推荐使用 [uv](https://github.com/astral-sh/uv) 加速依赖安装

### 1.2 安装 uv（可选但推荐）

```bash
# Linux/macOS
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows (PowerShell)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 1.3 一键安装

```bash
# Linux/macOS
chmod +x start.sh && ./start.sh

# Windows
start.bat
```

### 1.4 手动安装

```bash
# 创建虚拟环境
uv venv .venv

# 激活虚拟环境
source .venv/bin/activate    # Linux/macOS
# .venv\Scripts\activate     # Windows

# 安装依赖
uv pip install -r requirements.txt

# 复制配置文件
cp .env.example .env

# 初始化数据库
python main.py initdb
```

---

## 二、配置说明

所有配置通过项目根目录下的 `.env` 文件管理。首次使用时从 `.env.example` 复制：

```bash
cp .env.example .env
```

### 2.1 数据库配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `DATABASE_URL` | `sqlite:///data/price_monitor.db` | SQLAlchemy 连接字符串 |

如需使用其他数据库（如 PostgreSQL），修改为：
```
DATABASE_URL=postgresql://user:password@localhost:5432/price_monitor
```

### 2.2 爬虫配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `CRAWL_CONCURRENCY` | `5` | 最大并发下载数 |
| `CRAWL_TIMEOUT` | `30` | 单次请求超时秒数 |
| `CRAWL_RETRY` | `3` | 失败重试次数 |
| `CRAWL_DELAY` | `1.0` | 重试间隔基数（秒，每次递增） |

### 2.3 监控配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `MONITOR_INTERVAL_MINUTES` | `30` | 定时监控间隔（分钟） |
| `PRICE_THRESHOLD` | `0` | 价格阈值，大于0时低于此价格触发提醒 |

**降价提醒触发条件**（满足任一即可）：
1. 当前价格低于 `PRICE_THRESHOLD`（当阈值 > 0 时）
2. 当前价格低于上次记录的价格

### 2.4 代理IP配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `PROXY_ENABLED` | `false` | 是否启用代理 |
| `PROXY_API_URL` | (空) | 代理API地址（需自行实现调用） |
| `PROXY_LIST` | (空) | 静态代理列表（逗号分隔） |

配置示例：
```env
PROXY_ENABLED=true
PROXY_LIST=123.45.67.89:8080,98.76.54.32:3128,http://proxy.example.com:1080
```

代理池特性：
- 从配置加载代理列表
- 下载失败时自动标记为不可用并剔除
- 下载成功时恢复之前被标记的代理
- 提供简易健康检查（通过 httpbin.org/ip 测试）

### 2.5 User-Agent 配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `UA_POOL_ENABLED` | `true` | 是否启用UA随机轮换 |

启用后，每次请求从内置6种主流浏览器UA中随机选择一个。禁用时固定使用第一个UA。

### 2.6 Cookie 配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `COOKIE_POOL_ENABLED` | `false` | 是否启用Cookie池 |
| `COOKIE_LIST` | (空) | Cookie列表（逗号分隔） |

配置示例：
```env
COOKIE_POOL_ENABLED=true
COOKIE_LIST=session_id=abc123,user_token=xyz789
```

### 2.7 验证码配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `CAPTCHA_ENABLED` | `false` | 是否启用验证码识别 |
| `CAPTCHA_API_URL` | (空) | 打码API地址 |
| `CAPTCHA_API_KEY` | (空) | 打码API密钥 |

默认关闭。启用后需在 `anti_detect/captcha.py` 的 `_call_captcha_api` 方法中实现具体的API调用逻辑。

### 2.8 邮件通知配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `NOTIFY_ENABLED` | `false` | 是否启用邮件通知 |
| `SMTP_HOST` | (空) | SMTP服务器地址 |
| `SMTP_PORT` | `465` | SMTP端口（465=SSL, 587=TLS） |
| `SMTP_USER` | (空) | SMTP用户名 |
| `SMTP_PASSWORD` | (空) | SMTP密码 |
| `SMTP_FROM` | (空) | 发件人地址（为空时使用SMTP_USER） |
| `NOTIFY_EMAILS` | (空) | 收件人列表（逗号分隔） |

配置示例（以QQ邮箱为例）：
```env
NOTIFY_ENABLED=true
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=your_email@qq.com
NOTIFY_EMAILS=receiver1@example.com,receiver2@example.com
```

### 2.9 解析配置（CSS选择器）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `SELECTOR_TITLE` | `h1.product-title` | 商品标题选择器 |
| `SELECTOR_PRICE` | `span.price` | 当前价格选择器 |
| `SELECTOR_ORIGINAL_PRICE` | `span.original-price` | 原价选择器 |
| `SELECTOR_STOCK` | `span.stock` | 库存选择器 |

根据目标网站的HTML结构调整选择器即可适配不同电商平台。

---

## 三、命令使用示例

### 3.1 initdb - 初始化数据库

```bash
python main.py initdb
```

创建 SQLite 数据库文件和所有表。幂等操作，可重复执行。

### 3.2 crawl - 一次性抓取

```bash
# 抓取单个商品
python main.py crawl https://www.example.com/product/10086

# 抓取多个商品（空格分隔）
python main.py crawl https://www.example.com/product/1 https://www.example.com/product/2

# 从文件读取URL
python main.py crawl -f urls.txt
```

URL文件格式（`urls.txt`）：
```
# 每行一个URL，#开头为注释
https://www.example.com/product/1
https://www.example.com/product/2
https://www.example.com/product/3
```

抓取流程：
1. 异步下载所有URL（并发控制 + 重试）
2. 解析HTML提取商品信息
3. 存入数据库（商品去重 + 价格记录）
4. 检测降价并触发通知

### 3.3 monitor - 定时监控

```bash
# 使用配置默认间隔（30分钟）
python main.py monitor -u https://www.example.com/product/1

# 多个URL（逗号分隔）
python main.py monitor -u https://www.example.com/product/1,https://www.example.com/product/2

# 自定义间隔（每15分钟）
python main.py monitor -u https://www.example.com/product/1 -i 15

# 从文件读取URL
python main.py monitor -f urls.txt -i 60
```

监控启动后会：
1. 立即执行一次抓取
2. 按 `MONITOR_INTERVAL_MINUTES`（或 `-i` 参数）定时重复抓取
3. 每次抓取后检测降价并发送通知
4. `Ctrl+C` 停止监控

### 3.4 query - 查询历史

```bash
# 列出所有已监控的商品
python main.py query

# 按URL查询价格历史
python main.py query -u https://www.example.com/product/1

# 按商品ID查询，显示最近50条记录
python main.py query --id 1 -n 50
```

输出示例：
```
商品: 小米14 Pro 16GB+512GB 黑色 5G智能手机
URL: https://www.example.com/product/10086

价格历史（最近 3 条）
时间                  价格        原价        库存
2024-01-15 10:30:00  ¥4999.00   ¥5499.00   有货
2024-01-14 10:30:00  ¥5099.00   ¥5499.00   有货
2024-01-13 10:30:00  ¥5299.00   ¥5499.00   有货
```

---

## 四、反爬策略说明

### 4.1 User-Agent 轮换

内置6种主流浏览器UA（Chrome/Firefox/Safari/Edge，覆盖Windows/macOS/Linux），每次请求随机选择。通过 `UA_POOL_ENABLED` 控制开关。

### 4.2 代理IP池

- 从 `.env` 的 `PROXY_LIST` 加载静态代理列表
- 支持从 `PROXY_API_URL` 动态获取（需自行实现API调用）
- 下载失败时自动标记代理为不可用并剔除
- 下载成功时恢复之前被标记的代理
- 提供 `health_check()` 方法进行批量健康检查

### 4.3 Cookie 池

从 `.env` 的 `COOKIE_LIST` 加载，每次请求随机选择一个Cookie，模拟不同用户会话。

### 4.4 验证码处理

默认关闭（`CAPTCHA_ENABLED=false`）。启用后提供占位接口，需在 `anti_detect/captcha.py` 中实现具体的打码API调用。

### 4.5 请求间隔与重试

- `CRAWL_DELAY`：重试间隔基数，每次递增（第1次重试等待1s，第2次2s，第3次3s）
- `CRAWL_RETRY`：最大重试次数（默认3次）
- `CRAWL_TIMEOUT`：单次请求超时（默认30s）

### 4.6 并发控制

使用 `asyncio.Semaphore` 控制最大并发数（`CRAWL_CONCURRENCY`，默认5），避免对目标服务器造成过大压力。

---

## 五、日志说明

日志系统使用 loguru，配置如下：

- **控制台输出**：彩色格式，包含时间、级别、模块、行号
- **文件输出**：`data/logs/price_monitor_YYYY-MM-DD.log`
- **滚动策略**：每天午夜滚动
- **保留时间**：30天
- **编码**：UTF-8

日志级别通过 `LOG_LEVEL` 配置（DEBUG/INFO/WARNING/ERROR）。

---

## 六、测试说明

### 6.1 运行测试

```bash
# 运行所有测试
python -m pytest tests/ -v

# 运行特定测试文件
python -m pytest tests/test_parser.py -v
python -m pytest tests/test_crud.py -v
```

### 6.2 测试内容

- **test_parser.py**：使用 `tests/sample_product.html` 测试商品解析器
  - 标题、价格、原价、库存、URL、SKU 解析
  - 空HTML、缺少字段等边界情况
- **test_crud.py**：使用 SQLite 内存数据库测试CRUD操作
  - 商品创建、去重、查询
  - 价格记录添加、最新价格获取、历史查询

### 6.3 自检命令

```bash
# 语法检查
python -m compileall .

# 数据库初始化
python main.py initdb

# 运行测试
python -m pytest tests/ -v
```

---

## 七、适配新网站

要适配新的电商网站，通常只需修改 `.env` 中的CSS选择器：

```env
SELECTOR_TITLE=h1.product-name
SELECTOR_PRICE=div.now-price span
SELECTOR_ORIGINAL_PRICE=div.old-price span
SELECTOR_STOCK=div.stock-status
```

如果网站结构复杂，可以扩展 `crawler/parser.py` 中的 `ProductParser` 类，添加自定义解析逻辑。

---

## 八、常见问题

### Q1: 如何启用邮件通知？

在 `.env` 中设置：
```env
NOTIFY_ENABLED=true
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASSWORD=your_smtp_password
NOTIFY_EMAILS=receiver@example.com
```

### Q2: 抓取被403拦截怎么办？

1. 启用UA轮换：`UA_POOL_ENABLED=true`
2. 启用Cookie池：`COOKIE_POOL_ENABLED=true` 并配置 `COOKIE_LIST`
3. 启用代理池：`PROXY_ENABLED=true` 并配置 `PROXY_LIST`
4. 降低并发数：`CRAWL_CONCURRENCY=2`
5. 增加重试间隔：`CRAWL_DELAY=3.0`

### Q3: 数据库文件在哪里？

默认在 `data/price_monitor.db`，可通过 `DATABASE_URL` 配置修改路径。

### Q4: 如何查看历史价格？

```bash
python main.py query -u <商品URL>
```

### Q5: 监控如何停止？

按 `Ctrl+C` 即可停止定时监控。

---

## 九、已知限制

1. 代理API动态获取功能为占位接口，需根据实际代理服务商API文档实现
2. 验证码识别为占位接口，需对接第三方打码平台
3. 解析器依赖CSS选择器配置，复杂页面可能需要自定义解析逻辑
4. SQLite 不适合高并发写入场景，大规模监控建议切换到 PostgreSQL/MySQL
5. 邮件通知为同步发送，大量通知可能阻塞事件循环（可改为异步发送）
